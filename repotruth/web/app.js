const form=document.querySelector('#scan-form');const target=document.querySelector('#target');const submit=document.querySelector('#submit');const statusBox=document.querySelector('#status');const resultBox=document.querySelector('#result');const tabs=[...document.querySelectorAll('.tab')];const hint=document.querySelector('#hint');let mode='github';
const esc=value=>String(value).replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
tabs.forEach(tab=>tab.addEventListener('click',()=>{mode=tab.dataset.mode;tabs.forEach(item=>item.classList.toggle('active',item===tab));target.value=mode==='github'?'https://github.com/flokiss42-source/repotruth':'';target.placeholder=mode==='github'?'https://github.com/owner/project':'C:\\path\\to\\repository';hint.textContent=mode==='github'?'Only public github.com repositories. No repository code is executed.':'The server reads the selected folder but never executes its code.';target.focus()}));
function render(data){document.querySelector('#repo-name').textContent=data.root;document.querySelector('#grade').textContent=data.grade;document.querySelector('#score').textContent=data.score;document.querySelector('#facts').textContent=`${data.facts.files_scanned} files · ${data.facts.test_files} tests · ${data.facts.ci_workflows} workflows · ${data.facts.evidence_contracts} contracts`;const container=document.querySelector('#findings');if(!data.findings.length){container.innerHTML='<article class="clean"><h3>No visible mismatch found</h3><p>The active deterministic rules found evidence consistent with this repository’s claims.</p></article>'}else{container.innerHTML=data.findings.map(item=>`<article class="finding ${esc(item.severity)}"><div class="meta">${esc(item.severity.toUpperCase())} · ${esc(item.rule_id)} · ${esc(item.path)}:${item.line}</div><h3>${esc(item.title)}</h3><p>${esc(item.message)}</p><p class="fix"><b>Fix:</b> ${esc(item.remediation)}</p></article>`).join('')}resultBox.classList.remove('hidden');resultBox.scrollIntoView({behavior:'smooth',block:'start'})}
form.addEventListener('submit',async event=>{event.preventDefault();submit.disabled=true;resultBox.classList.add('hidden');statusBox.className='status';statusBox.textContent=mode==='github'?'Downloading and examining repository evidence…':'Examining local repository evidence…';try{const response=await fetch('/api/scan',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({mode,value:target.value})});const payload=await response.json();if(!response.ok)throw new Error(payload.error||'Scan failed');statusBox.classList.add('hidden');render(payload.result)}catch(error){statusBox.className='status error';statusBox.textContent=error.message}finally{submit.disabled=false}});
